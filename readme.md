# Log Query

Aplicação de terminal desenvolvida em Node.js para processamento e consulta de arquivos de logs.

O projeto lê um arquivo de logs no formato JSON Lines, processa os registros utilizando streams, armazena os dados em um banco SQLite e utiliza Inteligência Artificial para transformar perguntas em linguagem natural em consultas SQL.

## Instalação

```
npm install
```
