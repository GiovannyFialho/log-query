{
  "name": "log-query",
  "version": "1.0.0",
  "scripts": {
    "start": "node --env-file=.env.local src/index.ts",
    "dev": "node --env-file=.env.local --watch src/index.ts",
    "setup": "npm install && npm run env:setup",
    "env:setup": "cp .env.example .env.local",
    "seed": "node src/ingest/seed.ts",
    "ingest": "node src/ingest/ingest.ts",
    "test": "node --test"
  },
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@faker-js/faker": "^10.6.0",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@ai-sdk/google": "^4.0.46",
    "ai": "^7.0.69",
    "zod": "^4.4.3"
  }
}
